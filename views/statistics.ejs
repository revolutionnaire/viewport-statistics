<!DOCTYPE html>
<html>
  <%- include('includes/head.ejs'); %>
<body id="statistics">
  <center>
    <h1>Averages</h1>
    <% if (averageViewport) { %>
      <table>
        <tr>
          <td>
            <h4 class="label">Desktop</h4>
            <hr />
            <h3 class="data"><%= averageViewport.desktop.size %><sup>px</sup></h3>
            <hr />
            <h4 class="data-secondary">Total of <%= averageViewport.desktop.count; %> visits</h4>
          </td>
          <td>
            <h4 class="label">Mobile</h4>
            <hr />
            <h3 class="data"><%= averageViewport.mobile.size %><sup>px</sup></h3>
            <hr />
            <h4 class="data-secondary">Total of <%= averageViewport.mobile.count; %> visits</h4>
          </td>
        </tr>
      </table>
    <% } else { %>
      <p>No viewport data available yet.</p>
    <% } %>
    <a href="/">Back to your dimensions</a>
  </center>
</body>
</html>
